#!/bin/bash

until [[ -p "/etc/service/nginx/supervise/ok" && -p "/etc/service/php-fpm/supervise/ok" ]]
do
  echo "waiting for runit to be runiting"
  sleep 1
done

sv start nginx
sv start php-fpm

while sleep 60; do false; done